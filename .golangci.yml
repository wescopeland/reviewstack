version: "2"

run:
  timeout: 5m
  tests: true

linters:
  default: standard
  exclusions:
    rules:
      - path: _test\.go
        linters:
          - errcheck

formatters:
  enable:
    - gofumpt
